%h1 Albums

%table
  - @albums.each do |album|
    %tr
      %h2= link_to album.name, album
      %h3= album.description
      %p
        - album.photos.each do |photo|
          = link_to image_tag(photo.photo_path, size: "270x180"), album_photo_path(album, photo.id)
      -#  - for i in 1..3 
      -#    = link_to image_tag(album.photos[i].photo_path, size: "270x180"), album.photos[i].photo_path
      = link_to 'Show', album
      \|
      = link_to 'Photos', album_photos_path(album)
      \|
      = link_to 'New Photo', new_album_photo_path(album)
      \|
      = link_to 'Edit', edit_album_path(album)
      \|
      = link_to 'Destroy', album, :method => :delete, :data => { :confirm => 'Are you sure?' }


= link_to 'New Album', new_album_path

%br

