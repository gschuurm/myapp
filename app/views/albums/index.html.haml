%h1 Albums

%table
  %tr
    %th Name
    %th Description
    %th
    %th
    %th

  - @albums.each do |album|
    %tr
      %td= album.name
      %td= album.description
      %td
        - album.photos.each do |photo|
          %a.th
            .lightbox_content
              = photo.photo_path
            = link_to image_tag(photo.photo_path, size: "45x30"), photo.photo_file 
            -#this doesn't work^ and this: :"data-lightbox" => ".lightbox_content"
      %td= link_to 'Show', album
      %td= link_to 'New Photo', new_album_photo_path(album)
      %td= link_to 'Edit', edit_album_path(album)
      %td= link_to 'Destroy', album, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Album', new_album_path


